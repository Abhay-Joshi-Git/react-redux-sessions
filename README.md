# react-redux-sessions
exercises for react redux sessions

There are separate self-containing folders. Once you are inside the the fodler, run following in order to run the app - 

```

npm install

npm start

```
